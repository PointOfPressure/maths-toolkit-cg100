# maths.py - launcher for the visual Maths Toolkit (OCR B Maths + Further Maths).
# Run THIS file. The app body is casui.py (imported), which loads the engine
# (caslex/caseng/casrender/cascalc) and the section modules on demand.
import casui
casui.main()
